# 20210823日总结

1. python编程细节：

   re.findall(正则表达式，待匹配字符串)，返回值是数组

   ACM模式接受同一行用空格隔开的整数：

   ```python
   inputList = list(map(int, input().split())).
   ```


2. 组合问题\[求取叶子结点，startIndex的使用，剪枝操作，去重]，分割问题\[截取的模拟，子串的选取]，子集问题\[遍历所有节点]，比较难懂需要多过几遍。
3. 开组会，和老板讨论论文。\[秋招和论文同时进行太难了，挺住！]

