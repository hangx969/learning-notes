apiVersion: v1
kind: Pod
metadata:
  name: nginx-pdb-test
  namespace: default
  labels: 
    app: "nginx-pdb"
spec:
  containers:
  - image: nginx:1.17.1
    name: pod
    ports:
    - name: nginx-port
      containerPort: 80
      protocol: TCP
