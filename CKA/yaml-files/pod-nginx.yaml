apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: default
  labels: 
    version: "1.0"
spec:
  containers:
  - image: nginx:1.17.1
    name: pod
    ports:
    - name: nginx-port
      containerPort: 80
      protocol: TCP
